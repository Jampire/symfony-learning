{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ label }}</h1>

    <div style="width: 400px;">
        {{ form(form) }}
    </div>

    {% if videos is defined and videos is not empty %}
        {% for v in videos %}
            {{ v.id }}
            {{ v.title }}
        {% endfor %}
    {%  endif %}
{% endblock %}
